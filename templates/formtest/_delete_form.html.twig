<form method="post" action="{{ path('app_formtest_delete', {'id': formtest.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formtest.id) }}">
    <button class="btn">Delete</button>
</form>
